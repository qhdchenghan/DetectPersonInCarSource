<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="ch.qhd.facedetectcamera">

    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="车内人员检测客户端"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="ch.qhd.bluetooth.MainActivity" android:screenOrientation="portrait"  >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" android:configChanges="keyboard|keyboardHidden|orientation|screenSize" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>